@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Product

@*@if (ViewBag.SubCategoryName != null)
{
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        @Html.DropDownList("SelectedValue", ViewBag.SubCategoryName as SelectList, "Select an option", new { @class = "dropdown-item form-control" })
    </div>
}
*@

@if (ViewBag.SubCategoryName != null)
{
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        @foreach (var item in ViewBag.SubCategoryName)
        {
            <a class="dropdown-item" href="@item.Value">@item.Text</a>
        }
    </div>
}


