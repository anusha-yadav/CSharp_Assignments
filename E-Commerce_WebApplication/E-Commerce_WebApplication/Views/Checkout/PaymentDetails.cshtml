@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model CheckoutViewModel

<form id="paymentDetails" method="post" action="/Checkout/ProcessPayment">
    <!-- Payment method selection -->
    <div class="form-group">
        <label for="PaymentMethod">Payment Method:</label>
        <select id="PaymentMethod" name="PaymentMethod" class="form-control" required>
            <option value="CreditCard">Credit Card</option>
            <option value="UPI">UPI</option>
            <option value="COD">Cash on Delivery</option>
        </select>
    </div>

    <!-- Credit card fields (you can include these if needed) -->
    <div class="form-group" id="creditCardFields">
        <!-- Credit card details fields here -->
        <!-- Example: Card Number, Expiration Date, CVV -->
        <label for="CardNumber">Card Number:</label>
        <input type="text" id="CardNumber" name="CardNumber" class="form-control">

        <label for="ExpirationDate">Expiration Date:</label>
        <input type="text" id="ExpirationDate" name="ExpirationDate" class="form-control">

        <label for="CVV">CVV:</label>
        <input type="text" id="CVV" name="CVV" class="form-control">
    </div>

    <button type="submit" class="btn btn-primary">Submit Payment</button>
</form>

<script>
    document.getElementById("PaymentMethod").addEventListener("change", function () {
        var selectedPaymentMethod = this.value;
        var creditCardFields = document.getElementById("creditCardFields");

        if (selectedPaymentMethod === "CreditCard") {
            creditCardFields.style.display = "block";
        } else {
            creditCardFields.style.display = "none";
        }
    });
</script>

